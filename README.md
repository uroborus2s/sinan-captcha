# sinan-captcha

图形验证码训练工程骨架。

当前仓库分为两条主线：

- `generator/`：Go 图形点选样本生成器
- `core/`：Python 训练与数据工程核心包

配套目录：

- `scripts/`：CLI 和 PowerShell 入口
- `tests/`：最小测试骨架
- `configs/`：运行配置
- `materials/`：素材清单和本地素材目录
- `datasets/`：运行时数据目录
- `reports/`：评估与 QA 报告

当前状态是“工程骨架初始化”，不包含完整业务实现。
