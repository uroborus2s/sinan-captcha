# 用户指南概览

本目录只保留面向阅读者目标的公开使用路径，不再混入维护者引导、阶段文档或私有过程材料。

当前固定两条主路径：

1. 使用项目编译结果
2. 搭建训练环境并完成模型训练

第二条路径在训练完成后继续衔接“训练完成后的模型使用与测试”，避免把“训练”与“训练后验收”混写在同一页。

维护者、实现者和阶段推进类资料已经从本目录主入口移出，避免把“如何使用项目”和“如何维护项目”混在一起。

- 本页是该目录的正文首页，用于说明范围、读者和维护边界。
- 目录树、页面路径和访问级别统一由根 `docs/index.md` 声明，这里不重复维护页面清单。
- 本目录下的 Markdown 页面、契约文件和资源文件应随内容变更一起演进。
